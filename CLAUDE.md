# 마음의 문 — 프로젝트 가이드

## 프로젝트 컨텍스트

한국 초등 고학년 대상 AI·디지털 윤리 교육 게임. 순수 HTML5 Canvas(런타임
의존성 0), GitHub Pages로 배포되는 PWA.

- **장르: 방탈출형 설득 어드벤처 (Escape Room Persuasion Adventure).**
  - 포켓몬식 수집/포획 요소가 아니다.
  - 문·잠금·이상현상·단서·장치 중심의 방탈출 거리를 탐험한다.
- **톤: 언더테일풍 다크(적막·고독·멜랑콜리 + 마른 유머). 포켓몬 요소 금지.**
  - 화면 노출 문자열에 `몬스터·도감·증표·-몬 작명` 금지 — validate의 어휘
    린트가 강제한다. 등장인물은 "조각/아이/인물"로 부른다.
  - 다크 ≠ 잔혹. 공포·유혈 금지, 스텔스 교육 층과 교사 도구는 항상 유지.
- 전투 문법은 **설득 배틀(마음 조각 배틀) 하나뿐**. 승패/HP/게임오버 같은
  전투 메타포 대신 마음이 열림/잠시 물러남 중심.
- 퀴즈는 **도전 극장(본편 밖)**에만 존재. 본편 메인 루프에는 퀴즈가 없다.
- 핵심 반전: 동행자 「반디」 = 영이(최종 보스)의 가면.
- **기준서: `docs/기준서-방탈출-리부트.md`** (2026-07-09 v4). 이 문서와
  기준서가 충돌하면 기준서를 우선한다.
- 구 v2 기획 문서(`기획-동행자-반디.md`, `스토리바이블-v2-마음의-문.md`)는
  역사 문서로 참고용 — 설계 기준으로 삼지 않는다.

## 배포 (중요)

- main에 머지되면 GitHub Actions가 Pages로 자동 배포한다.
- 서비스워커가 **cache-first**라 캐시 버전이 안 바뀌면 클라이언트가 옛
  버전을 계속 본다. **게임 자산(index.html·src/*.js·manifest·icons)을
  수정했으면 커밋 전에 반드시 `npm run bump`** — sw.js의 CACHE 버전을
  자산 해시로 갱신한다. 잊으면 `npm run validate`(및 CI)가 실패한다.

## 명령

| 명령 | 용도 |
|---|---|
| `npm run validate` | 데이터·어휘 린트·캐시 해시 검증 (수정 후 항상) |
| `npm test` | 스모크(800+) + 슬롯 테스트 |
| `npm run bump` | sw.js 캐시 버전을 자산 해시로 갱신 |
| `npm run docs` | 퀴즈 변경 시 주제별-문제-목록.md 재생성 |

## 워크플로

- 작업 브랜치 → PR → main 머지. main 직접 푸시 금지.
- 퀴즈(QUIZZES) 텍스트를 바꾸면 `npm run docs`도 함께 (validate가 검사).
- 세이브 스키마를 바꾸면 `migrateSlotV3` 패턴처럼 구 세이브 승계를 붙인다.
